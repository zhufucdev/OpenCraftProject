/**
 * To query a Minecraft Object like Player or Block
 * @param pattern {string}
 * @example
 * "#Xyz" to refer to the Entity whose name is 'Xyz', "213,65,72@world" for the Block
 * in world "world" whose location is x=213,y=65,z=72
 */
// TODO
function query(pattern) {
    if (pattern.startsWith('#')) {

    }
}
module.shareContext = false;
module.exports = query;