group 'com.zhufu.opencraft'

def langRoot = rootDir.path + '/server/plugins/lang'
delete(langRoot)
file(projectDir.path + '/src').listFiles().each { f ->
    println 'Copy ' + f.name
    copy {
        from f.path
        into langRoot
    }
}